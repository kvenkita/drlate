% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compliers.R
\name{kappa_weights}
\alias{kappa_weights}
\title{Abadie's kappa weights}
\usage{
kappa_weights(object, normalize = TRUE)
}
\arguments{
\item{object}{A fitted \code{\link[=drlate]{drlate()}} object (with \code{keep_data = TRUE}) using an
instrument propensity score (any \code{method} except \code{"ra"}).}

\item{normalize}{Logical. If \code{TRUE} (default), the returned weights are the
sampling-weighted, normalized weights \eqn{w\kappa / \sum w\kappa} that sum
to one (the form used to compute complier averages). If \code{FALSE}, the raw
kappa values are returned.}
}
\value{
A numeric vector with one entry per estimation-sample observation.
}
\description{
Returns the per-observation Abadie kappa weight implied by a fitted
\code{\link[=drlate]{drlate()}} object,
\deqn{\kappa = 1 - \frac{D(1 - Z)}{1 - p(X)} - \frac{(1 - D) Z}{p(X)},}
where \eqn{p(X)} is the estimated instrument propensity score. The kappa
weights identify the complier subpopulation: for any function \eqn{g} of the
data, \eqn{E[g \mid \mathrm{complier}] = E[\kappa g] / E[\kappa]}
(Abadie 2003). They are the weights used by \code{\link[=complier_means]{complier_means()}} and are the
\proglang{Stata} \code{estat compliers, genkappa()} object.
}
\examples{
fit <- drlate(lwage ~ age + educ, nvstat ~ age + educ,
              rsncode ~ age + educ, data = drlate_sim)
head(kappa_weights(fit))
}
\seealso{
\code{\link[=complier_means]{complier_means()}}
}
