% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr-hausman.R
\name{dr_hausman}
\alias{dr_hausman}
\title{Doubly robust Hausman test of unconfoundedness}
\usage{
dr_hausman(
  outcome,
  treatment,
  instrument,
  data,
  omodel = c("linear", "logit", "poisson"),
  tmodel = c("logit", "linear", "poisson"),
  ivmodel = c("logit", "ipt"),
  weights = NULL,
  cluster = NULL,
  pstolerance = 1e-05,
  subset = NULL
)
}
\arguments{
\item{outcome}{A formula \code{y ~ covariates} for the outcome model. Use
\code{y ~ 1} for no covariates (required when \code{method = "ipw"}).}

\item{treatment}{A formula \code{d ~ covariates} for the treatment model.}

\item{instrument}{A formula \code{z ~ covariates} for the instrument
propensity score model; \code{z} must be binary 0/1. Use \code{z ~ 1} when
\code{method = "ra"}.}

\item{data}{A data frame containing all variables.}

\item{omodel}{Outcome model family: \code{"linear"} (default; continuous),
\code{"logit"} or \code{"probit"} (outcome must be 0/1), \code{"poisson"} (outcome must
be non-negative), or \code{"flogit"} / \code{"fprobit"} (fractional outcome in
\verb{[0, 1]}, e.g. a proportion). The \code{f}-prefixed families share all
estimation with \code{"logit"} / \code{"probit"} and only relax the response to the
unit interval, matching the \pkg{Stata} \code{lateffects} \code{omodel} options.}

\item{tmodel}{Treatment model family: \code{"logit"} (default; treatment must
be 0/1), \code{"probit"}, \code{"linear"}, or \code{"poisson"}.}

\item{ivmodel}{Instrument propensity score model for the LATT half:
\code{"logit"} (default) or \code{"ipt"}.}

\item{weights}{Optional sampling weights (a numeric vector, or a column
name in \code{data} given as a string).}

\item{cluster}{Optional cluster identifier for clustered standard errors
(a vector, or a column name in \code{data} given as a string).}

\item{pstolerance}{Overlap tolerance: estimation stops with an error if
any estimated instrument propensity score is below \code{pstolerance} or
above \code{1 - pstolerance}. Default \code{1e-5}.}

\item{subset}{Optional logical or integer vector selecting rows of \code{data}.}
}
\value{
An object of class \code{"htest"} with the z statistic, p-value, and
the DR LATT, DR ATT, and difference estimates.
}
\description{
Tests whether the treatment is unconfounded given the covariates, using
the comparison proposed by Słoczyński, Uysal, and Wooldridge (2022,
Section 5), building on Donald, Hsu, and Lieli (2014). Under
\strong{one-sided noncompliance} (nobody takes the treatment without the
instrument: \eqn{\Pr(D = 1 \mid Z = 0) = 0}), the LATT identified
through the instrument equals the ATT identified through
unconfoundedness of the treatment — so a significant difference between
the doubly robust LATT estimate (which uses the instrument) and the
doubly robust ATT estimate (which does not) is evidence against
unconfoundedness. Unlike the textbook OLS-vs-IV Hausman test, this
comparison is robust to treatment effect heterogeneity.
}
\details{
The DR ATT estimator follows the paper's equation (33): a treatment
propensity score \eqn{\Pr(D = 1 \mid X)} is fitted by logit QMLE on the
treatment-equation covariates; the outcome model is fitted on the
untreated sample weighted by the odds \eqn{\hat p/(1-\hat p)}; and
\eqn{\hat\tau_{ATT}} is the treated-sample mean outcome minus the mean
imputed counterfactual. The standard error of the difference comes from
stacking the moment conditions of \emph{both} estimators (and the difference)
into one M-estimation system, so the covariance between them is
accounted for analytically — the analytic option suggested in the paper.

Note that the two halves adjust on their respective formulas: the LATT
half's propensity score uses the \emph{instrument}-equation covariates,
while the ATT half's uses the \emph{treatment}-equation covariates (both
share the outcome model). Supply the same covariate set to all three
formulas unless you intend them to differ.
}
\examples{
d <- drlate_sim
d$nvstat[d$rsncode == 0] <- 0L   # impose one-sided noncompliance
dr_hausman(lwage ~ age + educ, nvstat ~ age + educ,
           rsncode ~ age + educ, data = d)

}
\references{
Słoczyński, T., S. D. Uysal, and J. M. Wooldridge (2022). "Doubly Robust
Estimation of Local Average Treatment Effects Using Inverse Probability
Weighted Regression Adjustment." \doi{10.48550/arXiv.2208.01300}

Donald, S. G., Y.-C. Hsu, and R. P. Lieli (2014). "Testing the
Unconfoundedness Assumption via Inverse Probability Weighted Estimators
of (L)ATT." \emph{Journal of Business & Economic Statistics} 32(3), 395-415.
}
