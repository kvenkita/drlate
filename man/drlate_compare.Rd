% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{drlate_compare}
\alias{drlate_compare}
\title{Compare drlate estimators in one call}
\usage{
drlate_compare(
  outcome,
  treatment,
  instrument,
  data,
  methods = c("ipwra", "ipw", "aipw", "ra"),
  both_norms = FALSE,
  ...
)
}
\arguments{
\item{outcome}{A formula \code{y ~ covariates} for the outcome model. Use
\code{y ~ 1} for no covariates (required when \code{method = "ipw"}).}

\item{treatment}{A formula \code{d ~ covariates} for the treatment model.}

\item{instrument}{A formula \code{z ~ covariates} for the instrument
propensity score model; \code{z} must be binary 0/1. Use \code{z ~ 1} when
\code{method = "ra"}.}

\item{data}{A data frame containing all variables.}

\item{methods}{Estimators to run.}

\item{both_norms}{Logical; also run the unnormalized variants of
\code{"ipw"} and \code{"aipw"} (default \code{FALSE}).}

\item{...}{Passed on to \code{\link[=drlate]{drlate()}} (e.g. \code{omodel}, \code{tmodel}, \code{ivmodel},
\code{estimand}, \code{weights}, \code{cluster}).}
}
\value{
An object of class \code{"drlate_compare"}: a data frame with columns
\code{method}, \code{normalized}, \code{estimate}, \code{se}, \code{ci_lo}, \code{ci_hi}, with a
\code{print} method and a dot-whisker \code{plot} method.
}
\description{
Runs several estimators on the same specification and collects the
causal estimates with their confidence intervals — the sensitivity
comparison applied papers routinely report. Formula restrictions are
handled automatically: \code{method = "ipw"} drops the outcome/treatment
covariates and \code{method = "ra"} drops the instrument covariates (each
with a message), matching the requirements of those estimators.
}
\examples{
cmp <- drlate_compare(lwage ~ age + educ, nvstat ~ age + educ,
                      rsncode ~ age + educ, data = drlate_sim)
cmp

}
