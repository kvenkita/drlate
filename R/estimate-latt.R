# LATT estimators: block dispatch per (method, normalization, ivmodel, case).
# Mirrors drlate_estimate_latt.ado. Implemented in Phase E.

#' @noRd
estimate_latt <- function(ctx, ps) {
  stop("estimand = \"latt\" is not implemented yet.", call. = FALSE)
}
